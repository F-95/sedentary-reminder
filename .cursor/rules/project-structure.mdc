---
alwaysApply: true
---

# 项目结构强制规范

本项目必须严格遵循以下目录结构与命名规则，任何文件/目录的创建、移动都不得违反：

## 1. 整体目录结构

```Plain Text
/（当前项目的根目录）
├── docs # [文档] 开发文档
│ ├── global/ # 全局文档
│ │ └── 项目信息.md/ # 项目基本信息文档
│ └── function/ # 功能文档
│   └── 功能清单.md/ # 功能清单文档
├── src/ # [前端] React 源代码（Vite 构建）
│ ├── assets/ # 静态资源（图片、字体等，经过 Vite 构建）
│ ├── components/ # 通用 React 组件（可复用于多页面 / 插件）
│ ├── pages/ # 主应用页面组件
│ ├── plugins/ # [扩展] 插件前端加载器与已安装插件的前端部分
│ │ ├── loader.tsx # 插件前端统一加载入口（禁止修改）
│ │ └── [plugin-id]/ # 具体已安装插件的前端代码（动态加载）
│ ├── utils/ # 前端工具函数（含 Tauri 命令封装）
│ ├── App.tsx # React 根组件（集成插件渲染区域）
│ ├── main.tsx # React 入口文件
│ └── vite-env.d.ts # Vite 类型声明
├── src-tauri/ # [后端] Rust + Tauri 核心代码
│ ├── src/ # Rust 源代码
│ │ ├── commands/ # Tauri 命令层（前端可调用的 API）
│ │ │ ├── mod.rs # 命令统一导出
│ │ │ ├── patch.rs # [扩展] 补丁升级相关命令
│ │ │ └── plugin.rs # [扩展] 插件管理相关命令
│ │ ├── core/ # 核心业务逻辑层
│ │ │ ├── mod.rs
│ │ │ ├── app_state.rs # 应用全局状态管理
│ │ │ └── config.rs # 应用配置管理
│ │ ├── extensions/ # [扩展] 核心扩展系统（禁止随意修改）
│ │ │ ├── mod.rs
│ │ │ ├── patch/ # 补丁升级系统
│ │ │ │ ├── mod.rs
│ │ │ │ ├── loader.rs # 补丁加载逻辑
│ │ │ │ └── executor.rs # 补丁执行逻辑
│ │ │ └── plugin/ # 插件系统
│ │ │ ├── mod.rs
│ │ │ ├── loader.rs # 插件加载逻辑
│ │ │ ├── registry.rs # 插件注册表
│ │ │ └── sandbox.rs # 插件安全沙箱
│ │ ├── plugins/ # [扩展] 已安装插件的后端代码（动态链接）
│ │ │ └── [plugin-id]/ # 具体已安装插件的 Rust 代码
│ │ └── main.rs # Tauri 入口文件（初始化扩展系统）
│ ├── tauri.conf.json # Tauri 配置文件（需配置补丁 / 插件目录权限）
│ ├── build.rs # Rust 构建脚本
│ └── Cargo.toml # Rust 依赖配置
├── patches/ # [扩展] 补丁包存储目录（运行时动态读取）
│ └── [version-tag]/ # 按版本号组织的补丁包（如 v1.0.1-hotfix）
│ ├── manifest.json # 补丁元信息（版本、适用平台、执行顺序）
│ ├── backend/ # 补丁后端代码（编译后的 .dll/.so/.dylib）
│ └── frontend/ # 补丁前端代码（编译后的 .js/.css）
├── plugins/ # [扩展] 插件包存储目录（运行时动态读取）
│ └── [plugin-id]/ # 按插件 ID 组织的插件包
│ ├── manifest.json # 插件元信息（ID、版本、权限、依赖）
│ ├── backend/ # 插件后端代码（编译后的动态库）
│ └── frontend/ # 插件前端代码（编译后的 .js/.css + 组件入口）
├── public/ # 前端静态资源（不经过 Vite 构建，直接复制）
├── .gitignore # Git 忽略规则（需忽略 patches/plugins 运行时文件）
├── package.json # 前端依赖配置
├── vite.config.ts # Vite 构建配置（需配置插件前端构建规则）
└── tsconfig.json # TypeScript 配置
```

## 2. 命名规则（强制约束）

- **目录名**：全小写 + 短横线（如 `user-profile`, `v1.0.1-hotfix`）
- **Rust 文件/模块**：全小写 + 下划线（如 `patch_loader.rs`, `plugin_sandbox`）
- **React 组件文件**：大驼峰（如 `PluginLoader.tsx`, `App.tsx`）
- **React 工具函数/普通文件**：小驼峰（如 `formatDate.ts`, `tauriCommands.ts`）
- **补丁包目录**：`v[主版本].[次版本].[修订号]-[后缀]`（如 `v1.0.1-hotfix`, `v1.1.0-feature`）
- **插件 ID**：全小写 + 点分隔（反向域名风格，如 `com.example.network-api`, `io.github.auth-plugin`）
- 基于Ant Design封装的通用业务组件，遵循React组件大驼峰命名规则，命名需体现业务属性，禁止直接覆盖AntD原生组件名（如禁止命名为`Button.tsx`，应命名为`PrimaryButton.tsx`/`FormSubmitButton.tsx`）

## 3. 扩展功能专项约束

### 3.1 补丁升级系统约束

- 补丁包必须包含 `manifest.json`，明确版本号、适用平台（`windows`/`macos`/`linux`/`all`）、依赖的基础版本、执行顺序；
- 补丁后端代码必须编译为对应平台的动态库，禁止修改核心 `src-tauri/src` 代码；
- 补丁前端代码必须编译为独立的 ES Module，通过 `src/plugins/loader.tsx` 动态加载，禁止直接修改 `src/pages`/`src/components`；
- 补丁执行必须支持回滚机制，`patches/[version-tag]/` 下需包含 `rollback.json` 回滚配置。

### 3.2 插件安装系统约束

- 插件包必须包含 `manifest.json`，明确插件 ID、版本、名称、描述、权限列表（如 `network:fetch`, `fs:read`）、依赖的其他插件；
- 插件后端代码必须编译为对应平台的动态库，通过 `src-tauri/src/extensions/plugin/` 沙箱加载，禁止直接访问核心业务逻辑；
- 插件前端代码必须编译为独立的 ES Module，暴露默认的 React 组件，通过 `src/plugins/loader.tsx` 动态渲染；
- 插件只能调用 `manifest.json` 中声明的 Tauri 命令，核心系统需通过 `sandbox.rs` 做权限校验；
- 已安装插件的后端代码统一放在 `src-tauri/src/plugins/[plugin-id]/`，前端代码统一放在 `src/plugins/[plugin-id]/`。

### 3.3 Ant Design UI 框架专项约束
- 全局主题配置必须统一在`src/App.tsx`中通过`ConfigProvider`实现，禁止在单组件内零散修改主题token，禁止覆盖AntD全局样式；
- 通用业务组件必须基于AntD原生组件封装，统一存放于`src/components/`目录，需附带完整TypeScript类型定义，禁止重复封装已有的原生组件能力；
- 页面级组件直接使用AntD原生组件，禁止在`src/pages/`目录下封装可复用的AntD业务组件；
- 插件/补丁前端代码使用AntD组件时，必须遵循沙箱隔离原则，禁止通过AntD修改全局样式影响主应用，禁止在插件内修改`ConfigProvider`全局配置；
- 禁止修改AntD内置的DOM结构、className，禁止使用深度选择器覆盖AntD组件默认样式，特殊定制需通过`ConfigProvider`主题token实现。

## 4. 新增/修改文件的通用约束

- 禁止直接修改 `src-tauri/src/extensions/` 下的核心扩展系统代码，如需调整需先提交架构评审；
- 新增 Tauri 命令必须放在 `src-tauri/src/commands/` 下，并在 `mod.rs` 中统一导出；
- 新增 React 通用组件必须放在 `src/components/` 下，需附带 TypeScript 类型定义和使用示例；
- 新增基于AntD的通用组件必须放在`src/components/`下，需附带TypeScript类型定义、使用示例和入参说明；
- 所有AntD组件的使用必须做异常边界处理，表单组件必须配套校验规则，禁止直接使用无约束的原生组件。
- `patches/` 和 `plugins/` 目录下的运行时文件禁止提交到 Git，需在 `.gitignore` 中明确配置；
- 所有代码必须附带清晰的中文注释，核心扩展逻辑需说明安全机制与边界条件。
