# 变更日志（Changelog）

本文件记录 **久坐提醒（Sedentary Reminder）** 各发布版本的更新摘要。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

<!-- 下一版本发布前：将此处条目移至新版 ## [x.y.z]，并保留本段为空或继续累积 -->

## [0.1.13] - 2026-05-16

### 构建与发布

- 对齐 Tauri 版本：`@tauri-apps/cli` 与 Rust `tauri` 均锁定 **2.10.1**，修复 `v0.1.12` Release CI 因 `tauri 2.11.1` 与 `@tauri-apps/api 2.10.1` 不一致导致的构建失败。

## [0.1.12] - 2026-05-16

### 用户可见

- **作者的哔哔赖赖**：文末展示微信公众号「乘澜 charlan」微信搜一搜横幅，便于扫码关注。

## [0.1.11] - 2026-05-16

### 用户可见

- **检查更新**：「发现新版本」弹窗缩短说明区（截断「### 构建…」及之后、轻量去掉 Markdown 噪音、超长兜底），说明区限高可滚动；必要时提供 **在 GitHub 查看完整说明** 链接。

### 构建与发布

- README 补充：面向应用内 updater 的 **Release 正文宜短**；技术细节可放 `CHANGELOG` 或「### 构建与发布」小节（应用内会截断该段）。

## [0.1.10] - 2026-05-14

### 用户可见

- **全屏久坐提醒**：屏蔽默认 **右键菜单**（返回/刷新等），避免误触 WebView 系统菜单。
- **主界面底部**：增加 **用户须知**（配置、统计与调度快照仅存本机，不上传云端）；附说明图标悬停可看完整文案。
- **久坐提醒设置**：可选 **解锁视为记录活动**（Windows / macOS）：系统从锁屏解锁时按「记录活动」重排下次久坐；**全屏提醒进行中**不会触发。
- **调度延续**：久坐「下次提醒」与补水「下一拍」的绝对时间写入应用数据目录 **`scheduler_snapshot.json`**，**重启或就地升级**后在配置未变且时间仍有效时尽量延续倒计时（卸载清空数据目录除外）。
- **主窗口**：默认高度 **800px**；久坐全屏结束后恢复主窗尺寸与之一致（宽 520 不变）。

### 构建与发布

- 新增 Tauri 命令 **`load_scheduler_snapshot` / `save_scheduler_snapshot`**；平台模块 **`session_unlock`**（Windows：`WTSRegisterSessionNotification` + 子类化；macOS：`Darwin` 通知中心订阅 `com.apple.screenIsUnlocked`）。
- 与标签 **`v0.1.10`**、**第十版** 功能对应；`package.json`、`Cargo.toml`、`tauri.conf.json` 对齐为 **0.1.10**。

## [0.1.9] - 2026-05-14

### 用户可见

- 主界面 **底部** 常驻展示当前 **版本号（v 前缀 + semver）**，旁附 **刷新图标**：一键 **检查更新**；发现新版本时可确认后 **下载并安装** 并在安装完成后 **自动重启**（行为随 Tauri Updater）。
- 检查更新失败时，对常见原因给出 **中文说明**（例如未发布 `latest.json`、地址不可达等）。
- **热修复（同版本 v0.1.9 安装包重建）**：修复「无新版本」时检查更新报错（`Cannot read properties of null (reading 'available')`），方式为对齐前端 `@tauri-apps/plugin-updater` 与 Rust `tauri-plugin-updater` 版本；**请从 Release 重新下载安装包**，或通过自动更新拉取新构建的 `latest.json` 与安装包。

### 构建与发布

- **Windows**：安装包配置 **WebView2 引导**（`downloadBootstrapper`）；生产构建前端目标调整为 **chrome90**，以适配较旧 WebView2。
- 集成 **Tauri Updater** 与 **process** 插件；`tauri.conf.json` 启用 **createUpdaterArtifacts**；默认更新地址指向 `F-95/sedentary-reminder` Releases 下的 **latest.json**（发版时需上传构建产物与签名）。
- **`get_app_info`** 使用 **Cargo 包版本** 与 **bundle identifier** 一致输出；**`scripts/version.mjs`** 可一键同步 `package.json` / `Cargo.toml` / `tauri.conf.json` 版本号。
- 与标签 **`v0.1.9`**、**`第九版`** 对应；第九版全套设计文档归档于 `docs/久坐提醒/第九版/`；仓库增加 **`.env.example`**（签名环境变量说明）与 **更新公钥** `src-tauri/keys/updater.key.pub`（私钥不入库）。

## [0.1.8] - 2026-05-12

### 用户可见

- 主设置 **久坐提醒** 区增加 **记录活动**、**跳过本次**：写入本地统计并从当前时刻重排下次久坐提醒（未到点也可主动推进周期）。
- **活动统计**：堆叠图展示 **活动次数**（久坐完成 + 枢纽记录活动）与 **未活动次数**（跳过）；合计展示久坐提醒次数、补水、主动活动、跳过；图表下方增加基于数据的简短情绪简报（本地模板）。
- 主卡片内嵌 **活动统计** 简报图与上述语义对齐（近 7 日双序列）。

### 构建与发布

- 与标签 `v0.1.8`、`第八版` 对应；`package.json`、`Cargo.toml`、`tauri.conf.json` 对齐为 **0.1.8**。
- 第八版设计文档归档于 `docs/久坐提醒/第八版/`。

## [0.1.7] - 2026-05-11

### 用户可见

- 主窗口与网页标题统一为 **久坐提醒（你的健康搭子）**。
- 主设置增加 **作者的哔哔赖赖** 入口，展示作者随笔与公众号署名。

### 构建与发布

- 与标签 `v0.1.7` 对应；`package.json`、`Cargo.toml`、`tauri.conf.json` 对齐为 **0.1.7**。
- 第七版设计文档归档于 `docs/久坐提醒/第七版/`。

## [0.1.6] - 2026-05-09

### 用户可见

- 浅色 / 深色 / 跟随系统主题；主窗口标题栏随主题同步（Tauri）。
- 活动统计：主设置卡片内嵌简报、详情按日/周/月/年；本地存储、无上传。
- 子页左上仅图标返回；主窗默认约 520×760，最小约 400×660。
- 枢纽顺序与 UI 细节优化（开机启动与主题模式行、与代码一致）。

### 构建与发布

- 与标签 `v0.1.6` 对应；`package.json`、`Cargo.toml`、`tauri.conf.json` 对齐为 **0.1.6**。
- 第六版设计文档已与当前实现对齐。

## [0.1.5] - 2026-05-09

### 用户可见

- 设置入口调整为「设置枢纽」布局，久坐相关配置独立成页。
- 「时段关怀」等标题与信息架构优化（第五版 UI）。

### 构建与发布

- 与标签 `v0.1.5` 对应之构建产物；详见 GitHub Releases。
- 将 `Cargo.toml`、`tauri.conf.json` 中的应用版本号与 `package.json` 对齐为 **0.1.5**（仓库维护，避免安装包与前端展示版本不一致）。

## [0.1.4] - 2026-05-09

### 用户可见

- **补水提醒**：可配置补水相关提醒与独立补水设置页。
- **免打扰**：支持折叠展示，交互与入口布局优化（第四版）。
- 与久坐提醒并列的入口与导航调整。

### 构建与发布

- 发布流程与文档示例中的版本号对齐为 `v0.1.4`。

## [0.1.3] - 2026-05-09

### 用户可见

- README 补充免打扰时段说明与配置存储 **v2** 行为说明，便于升级用户理解迁移。

### 构建与发布

- GitHub Release 默认为**正式发布**（非草稿）；归档久坐提醒第三版设计文档相关说明。
- CI：`release` 工作流权限与 `GITHUB_TOKEN` 403 排查说明补充。
- 版本号递增至 `0.1.3`，用于新标签触发 Release（避免与既有标签冲突）。

## [0.1.2] - 2026-05-08

### 用户可见

- 久坐提醒**第二版**：免打扰独立页等与产品文档对齐的行为与说明。

### 构建与发布

- 与标签 `v0.1.2` 对应之构建产物。

## [0.1.1] - 2026-04-30

### 用户可见

- **系统托盘**：鼠标悬停时展示当前提醒状态与**下次提醒时间**。

### 构建与发布

- CI：修正 `tauri-action` 引用版本与参数；支持 `workflow_dispatch` 补跑 Release。

## [0.1.0] - 2026-04-30

### 用户可见

- 首版可通过 Windows 安装包（GitHub Releases）分发的形态。
- 集成**系统通知**插件能力；优化主窗口关闭行为。
- 提醒设置页文案与多显示器相关逻辑优化。

### 构建与发布

- 启用 Windows 打包与 `release` GitHub Actions 工作流。
- 文档补充 Releases 使用说明；修正 `rust-cache` 工作区路径。
- 仓库元数据：`.gitignore`、README、Apache-2.0 许可证等。

---

安装包格式（NSIS / MSI）与 SmartScreen 说明见仓库根目录 [README.md](README.md)。
