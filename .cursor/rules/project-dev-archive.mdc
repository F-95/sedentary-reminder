---
alwaysApply: true
---

# 开发产物归档强制规则

本规则为开发全流程所有产物的归档标准，与智能体调用时机完全对齐，确保所有交付物可追溯、可复用、可审计，与设计文档归档规则完全对齐。

## 1. 代码产物归档规则

1.  开发完成后，所有代码必须严格遵循项目目录结构规范，提交至对应代码仓库，分支命名必须与指令完全一致「XXX功能-第X版」，禁止随意命名分支。
2.  代码提交必须保留完整的提交记录，提交信息必须遵循「功能版本+变更类型+变更内容+对应设计文档章节」的格式，禁止无意义的提交信息。
3.  最终验收通过的代码，必须合并至主分支，同时打版本标签，标签名与设计文档版本号完全一致「第X版」，确保版本可追溯。

## 2. 开发文档归档规则

1.  开发全流程每个智能体输出的所有报告，必须完整归档至指令匹配的`/docs/XXX功能/第X版/`目录下，与7份设计文档一并留存。
2.  迭代开发的《迭代影响评估报告》《变更记录对比表》《回归测试报告》《回滚方案验证报告》，必须完整归档至对应迭代版本的文档目录下。
3.  所有归档文档必须使用标准化Markdown格式，命名规范为「文档名称 - 第X版.md」，与设计文档命名规范完全一致。

## 3. 交付物清单归档规则

1.  开发闭环后，系统架构专家必须输出完整的《项目交付物清单》，明确标注所有交付物的名称、版本、存储路径、责任人、归档时间，归档至对应版本的文档目录下。
2.  交付物清单必须包含以下内容：设计文档清单、代码仓库地址、分支/标签信息、开发过程报告、测试报告、验收报告、构建产物信息、部署说明。
3.  所有交付物必须确保完整性，无缺失、无损坏，可直接用于后续迭代、部署、运维。

## 4. 归档校验规则

1.  所有归档内容完成后，必须由系统架构专家执行归档校验，确认所有交付物完整、命名规范、路径正确、与版本号匹配，输出《归档校验报告》。
2.  归档校验通过后，方可完成最终闭环；校验不通过，必须按要求补充完善后重新校验，禁止未通过归档校验就完成项目闭环。
3.  已归档的内容禁止私自修改、覆盖、删除，若需修改必须走变更流程，更新版本号后重新执行启动指令、归档，保留历史版本完整记录。
