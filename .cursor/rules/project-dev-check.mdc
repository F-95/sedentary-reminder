---
alwaysApply: true
---

# 开发环节校验与准入强制规则

本规则为开发全流程各环节的硬性校验标准，是智能体调用、环节流转的唯一准入依据，未通过对应校验，绝对禁止进入下一流程、调用下一环节智能体。

## 1. 后端代码提交强制校验（Rust后端专家环节准入闭环标准）

Rust后端代码提交前，必须强制执行以下4项校验，全部通过方可提交，缺一不可：

1.  格式校验：Rustfmt格式校验零告警，代码风格完全符合项目规范。
2.  语法校验：Clippy全量检查零告警，无语法错误、无安全隐患、无不符合规范的代码。
3.  编译校验：cargo build编译零错误，无依赖冲突、无兼容性问题。
4.  测试校验：单元测试全量通过，核心逻辑测试覆盖率≥90%，输出《单元测试报告》。

## 2. 前端代码提交强制校验（React前端专家环节准入闭环标准）

React前端代码提交前，必须强制执行以下4项校验，全部通过方可提交，缺一不可：

1.  格式校验：Prettier格式校验零告警，代码风格完全符合项目规范。
2.  语法校验：ESLint全量检查零告警，无语法错误、无不符合规范的代码。
3.  类型校验：TypeScript严格模式零类型错误、零any类型，全链路类型覆盖。
4.  构建校验：生产环境构建零错误，无依赖冲突、无包体积异常、无兼容性问题。

## 3. 联调环节准入规则（前后端协同联调智能体调用前置条件）

前后端联调启动前，必须满足以下准入条件，缺一不可：

1.  前后端代码均已完成各自的提交校验，输出对应的开发完成报告。
2.  后端接口已完成单机自测，所有接口可正常调用、请求/响应格式完全符合设计文档要求。
3.  前端已完成页面静态开发、Mock数据自测，所有交互逻辑可正常触发。
4.  系统架构专家已完成前后端接口契约1:1对齐校验，输出《接口契约校验报告》，无接口定义冲突。

## 4. 测试环节准入规则（测试专家调用前置条件）

测试工作启动前，必须满足以下准入条件，缺一不可：

1.  前后端联调全量通过，输出《联调完成报告》，所有接口联调无异常。
2.  核心功能全流程可正常跑通，无阻断性问题。
3.  测试专家已完成测试用例、自动化测试脚本的编写，测试环境已搭建完成。
4.  开发团队已提交完整的开发交付物清单、自测报告、联调报告，经用户确认。

## 5. 验收环节准入规则（产品专家调用前置条件）

最终需求验收启动前，必须满足以下准入条件，缺一不可：

1.  全量测试用例100%执行完成，P0-P1级缺陷全部闭环，输出《测试通过报告》。
2.  所有开发内容100%匹配设计文档、需求规格说明书的要求，无遗漏、无偏离。
3.  所有交付物已完整归档，包括代码、文档、测试报告、变更记录等。
4.  生产环境构建验证通过，可正常打包生成三端安装包，无构建异常。
