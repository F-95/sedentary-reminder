---
alwaysApply: true
---

# 老功能迭代开发专项规则

本规则适用于已有线上代码、基于历史版本迭代修改的开发场景，与顶层总则、设计文档规则完全对齐，明确迭代开发全流程的智能体调用时机与执行标准。

## 1. 迭代开发前置准入规则

启动指令执行后，必须完成以下4项准入校验，缺一不可，未通过校验绝对禁止调用任何开发类智能体：

1.  文档校验：指令匹配路径`/docs/XXX功能/第X版/`下，迭代版本7份全套设计文档完整、无缺失、已完成用户确认与内容锁定，明确标注迭代变更点、与历史版本的差异、影响范围。
2.  历史版本校验：已匹配到对应功能的历史稳定版本代码、设计文档，可正常访问、无损坏，作为迭代比对基准。
3.  会话校验：当前会话为全新独立会话，仅执行过一条合法启动指令，无其他功能的上下文混杂，会话名称与指令完全匹配。
4.  项目环境校验：项目现有代码可正常编译运行，开发环境、工具链配置完整，符合迭代开发要求。

## 2. 智能体调用时机明细（严格按顺序执行）

| 调用顺序 | 智能体角色                         | 触发前置条件                                 | 核心执行职责                                                                                                                                                                          | 必须输出物                                                                         | 闭环标准                                                     |
| -------- | ---------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| 1        | 系统架构专家                       | 启动指令解析完成、前置准入校验全部通过       | 1. 完成历史版本与迭代版本的全量比对，明确迭代变更范围、依赖关系、风险点；2. 评审迭代设计文档的开发适配性；3. 输出迭代架构约束、影响评估、回滚方案标准                                 | 《迭代影响评估报告》《开发适配性评审报告》《迭代架构约束标准》《回滚方案模板》     | 用户明确确认「评审通过，同意开启迭代开发」                   |
| 2        | Rust后端专家                       | 系统架构专家评审通过、用户确认开启迭代开发   | 1. 仅针对迭代变更点，100%匹配迭代版《后端详细设计（Rust）》完成代码开发；2. 禁止修改与迭代无关的历史代码；3. 完成编译校验、格式校验、变更内容单元测试；4. 输出迭代后接口契约          | 《迭代后端开发完成报告》《变更单元测试报告》《迭代后端接口契约》《代码变更对比表》 | 系统架构专家架构合规评审通过、用户明确确认「本环节验收通过」 |
| 3        | React前端专家                      | Rust后端专家环节验收闭环、迭代接口契约已锁定 | 1. 仅针对迭代变更点，100%匹配迭代版《前端详细设计（React）》、后端接口契约完成代码开发；2. 禁止修改与迭代无关的历史代码；3. 完成构建校验、格式校验、类型校验；4. 完成变更内容Mock自测 | 《迭代前端开发完成报告》《代码变更对比表》                                         | 系统架构专家架构合规评审通过、用户明确确认「本环节验收通过」 |
| 4        | Rust后端专家+React前端专家（协同） | 前后端迭代开发环节均验收闭环                 | 1. 完成迭代变更内容的接口联调；2. 完成历史核心功能的兼容性验证；3. 解决联调中的适配问题；4. 验证迭代业务闭环与历史功能兼容性                                                          | 《迭代联调完成报告》《兼容性验证报告》《接口契约校验报告》                         | 联调全量通过、兼容性无问题、用户明确确认「本环节验收通过」   |
| 5        | 测试专家                           | 联调与兼容性验证环节验收闭环                 | 1. 基于迭代版《测试与验收方案》执行变更内容专项测试；2. 执行历史核心功能全量回归测试；3. 编写自动化测试脚本；4. 输出缺陷清单并推动闭环；5. 完成回归测试                               | 《迭代测试报告》《回归测试报告》《缺陷清单》《迭代测试通过报告》                   | P0-P1级缺陷全部闭环、用户明确确认「本环节验收通过」          |
| 6        | 产品专家                           | 测试环节验收闭环                             | 1. 基于迭代版《需求规格说明书》完成迭代功能需求符合性验收；2. 确认历史功能不受影响；3. 输出最终验收结论                                                                               | 《迭代需求验收报告》                                                               | 用户明确确认「最终验收通过」                                 |
| 7        | 系统架构专家                       | 最终需求验收通过                             | 1. 完成回滚方案演练验证；2. 完成全量迭代交付物的归档合规性校验；3. 确认所有变更可追溯、符合规范；4. 输出归档校验报告                                                                  | 《回滚方案验证报告》《归档校验报告》《全流程合规性校验报告》                       | 归档校验全部通过、回滚方案验证通过                           |

## 3. 迭代变更管控规则

1.  迭代开发过程中，若需变更已锁定的迭代版设计文档内容，必须立即终止当前开发流程，重新完成设计文档的变更评审、用户确认、内容锁定，更新版本号后，重新执行启动指令开启新会话开发。
2.  迭代开发完成后，必须同步归档迭代变更记录、代码修改对比、影响范围说明至对应版本的文档目录，与设计文档一并留存，确保全流程可追溯。
3.  所有迭代变更必须保留完整的Git提交记录，提交信息必须明确标注「迭代版本号+变更内容+对应设计文档章节」，禁止无说明的代码提交。

## 4. 兼容性与回滚强制规则

1.  迭代开发必须保证历史功能的兼容性，若存在不兼容变更，必须在设计文档中明确标注，且经用户确认后方可实现，禁止无说明的不兼容变更。
2.  迭代开发必须配套完整的回滚方案，若上线后出现问题，可快速回滚至历史稳定版本，禁止无回滚方案的迭代开发上线。
