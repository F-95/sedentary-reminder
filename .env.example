# 中文注释：复制为 .env 后本地使用（.env 已加入 .gitignore，勿提交私钥）。
# 第九版起：启用 bundle.createUpdaterArtifacts 时，发布构建需设置以下变量（也可用系统/CI 环境变量，勿依赖 .env 参与 Tauri 签名）。
# TAURI_SIGNING_PRIVATE_KEY=
# TAURI_SIGNING_PRIVATE_KEY_PASSWORD=
#
# GitHub Actions：请在仓库 Settings → Secrets and variables → Actions 中配置同名 Repository secrets
#（与 README「发布与 CI」一节一致），否则 release 工作流会在「构建并发布 Release」前失败。
