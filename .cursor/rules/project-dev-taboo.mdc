---
alwaysApply: true
---

# 开发环节绝对禁忌红线

本规则为开发环节零容忍红线，任何智能体、任何环节违反任意一条，对应开发工作均视为无效，必须无条件返工，情节严重的需终止当前开发流程，重新执行合法启动指令。

1.  绝对禁止使用非固定格式指令启动任何开发任务，禁止简写、错写、修改启动指令格式。
2.  绝对禁止在无锁定设计文档、无合法启动指令的情况下，启动任何开发工作、调用任何开发类智能体。
3.  绝对禁止未按规定的调用时机、前置条件调用智能体，禁止提前调用、跳步调用、越权调用智能体。
4.  绝对禁止开发内容与对应版本锁定设计文档的要求不符，私自变更、偏离、删减设计文档定义的任何内容。
5.  绝对禁止在同一会话中执行多条启动指令、处理多个功能、多个版本的开发任务，禁止上下文混杂。
6.  绝对禁止上一开发环节未完成验收闭环，就跳步进入下一环节、调用下一环节智能体。
7.  绝对禁止未完成格式校验、编译检查、单元测试的代码，进入联调、测试环节。
8.  绝对禁止在开发过程中，私自修改项目核心架构、目录结构、全局规范，若需修改必须重新走设计文档评审与确认流程，重新执行启动指令。
9.  绝对禁止在老功能迭代过程中，私自修改与迭代需求无关的历史代码，禁止出现无文档记录的代码变更。
10. 绝对禁止输出无注释、无错误处理、无测试覆盖、与设计文档不符的无效代码。
11. 绝对禁止隐瞒开发过程中的缺陷、风险、技术债务，禁止未闭环的缺陷进入验收环节。
12. 绝对禁止未完成归档校验、无完整交付物的情况下，完成开发闭环。
