
### 用户可见

- README 补充免打扰时段说明与配置存储 **v2** 行为说明，便于升级用户理解迁移。

### 构建与发布

- GitHub Release 默认为**正式发布**（非草稿）；归档久坐提醒第三版设计文档相关说明。
- CI：`release` 工作流权限与 `GITHUB_TOKEN` 403 排查说明补充。
- 版本号递增至 `0.1.3`，用于新标签触发 Release（避免与既有标签冲突）。

---

Windows 安装包见下方 **Assets**。NSIS（`*-setup.exe`）为安装向导，MSI（`*.msi`）为企业/静默部署常用格式。SmartScreen 等与安装相关说明见本仓库 README「Windows 安装包（GitHub Releases）」一节。