---
alwaysApply: true
---

# 设计文档全局规则

本规则为项目所有设计文档、全流程角色协同必须100%遵守的顶层通用约束，覆盖设计全生命周期，任何环节、任何文档都不得违反，违者需重新执行对应环节。

## 1. 文档核心边界总规则

1.  严格执行「**一个功能的一个版本，对应7个独立的设计文档**」原则，禁止将7大文档合并到同一份文档中，禁止单章节内容拆分出多个独立文档，确保每个文档权责清晰、内容独立，解决单文档过长易出错的问题。
2.  每个独立文档必须遵循《设计文档固定大纲》中对应的固定不变的章节大纲，所有同类型文档的章节结构、顺序、必填项完全统一，禁止私自调整大纲顺序、删减必填章节、新增非规范章节，确保全项目所有功能设计文档的大纲一致性。
3.  文档所有内容必须完全适配项目「Tauri 2.0+ + Rust + React + Vite」技术栈，与已定义的项目目录结构规范100%对齐，禁止输出与现有项目架构冲突的设计内容。
4.  文档所有内容必须以「AI可直接编码落地」为核心目标，只保留有明确约束、可执行、无歧义的确定性信息，禁止输出空泛的理论描述、无落地价值的套话、未明确的备选方案。
5.  **设计环节编程行为绝对禁止规则**：本规则覆盖全文档设计全流程，仅允许开展设计方案梳理、文档编写、需求与架构论证工作，**全程禁止任何编程相关操作与内容输出**，包括但不限于：
    - 禁止编写、生成、输出任何源代码片段（Rust/React/TypeScript/Shell等）
    - 禁止输出依赖安装命令、构建命令、测试命令、CLI执行指令等任何可执行命令
    - 禁止调用文件系统工具写入/修改代码文件、执行代码相关操作
    - 禁止在设计文档中写入代码级实现细节、函数逻辑、组件完整代码等内容
    - 禁止介入代码调试、bug修复、前后端联调等开发环节相关工作

## 2. 独立文档总览与权责规则

以下7个独立文档为项目强制必填文档，全项目所有功能设计必须完整覆盖，不得缺项，每个文档的负责角色、必填性、核心定位固定不变，禁止越权输出非职责范围内的文档内容。

| 文档序号 | 独立文档固定名称         | 负责角色      | 必填性 | 核心定位                                                                 |
| -------- | ------------------------ | ------------- | ------ | ------------------------------------------------------------------------ |
| 文档1    | 文档元信息与全局开发共识 | 系统架构专家  | ★ 必须 | 全流程设计的全局约束基准，所有后续文档必须严格遵循本文档定义的规范       |
| 文档2    | 需求规格说明书           | 产品专家      | ★ 必须 | 功能需求的唯一基准，所有设计、开发、测试工作必须严格遵循本文档定义的需求 |
| 文档3    | 系统总体设计             | 系统架构专家  | ★ 必须 | 前后端设计的对齐基准，明确整体架构、模块边界、数据流转规则               |
| 文档4    | 后端详细设计（Rust）     | Rust后端专家  | ★ 必须 | 后端开发的唯一设计基准，明确接口、数据结构、业务逻辑的设计规范           |
| 文档5    | 前端详细设计（React）    | React前端专家 | ★ 必须 | 前端开发的唯一设计基准，明确页面、组件、交互、接口请求的设计规范         |
| 文档6    | 测试与验收方案           | 测试专家      | ★ 必须 | 测试执行与质量验收的唯一基准，明确测试范围、用例、准入准出标准           |
| 文档7    | 迭代与交付规范           | 全角色协同    | ★ 必须 | 功能迭代、构建、发布、回滚的执行规范，明确交付全流程的标准化规则         |

## 3. 全流程执行强制规则

1.  严格执行「**文档序号顺序执行，上一文档未确认、未锁定，绝对禁止进入下一文档**」的原则，禁止跳步、逆序、并行执行。
2.  每个文档必须由对应负责角色的智能体输出独立文档，文档必须严格遵循《设计文档固定大纲》，其他角色仅可提出补充建议，禁止越权输出非职责范围内的章节内容，禁止修改文档固定大纲。
3.  每个文档输出完成后，必须引导用户与对应专家完成讨论、修改、优化，直至用户明确输出「本文档确认无异议，可进入下一环节」的指令，方可**锁定本文档内容与文件**，禁止后续任何环节修改、覆盖、替换已锁定的前置文档，解决内容被误替换的问题。
4.  后续文档的设计必须严格基于前面已确认、已锁定的文档内容，禁止出现前后内容冲突、需求理解偏差的问题；如需修改已确认的前置文档内容，必须终止当前流程，重新从修改后的文档开始执行，同步更新对应文档的版本号。
5.  每个文档仅输出对应独立设计文档，不得额外生成其他文档，解决单文档单独生成额外文档的问题。

## 4. 文档存储与版本管理规则

1.  所有设计文档必须统一存储到项目根目录的 `/docs` 目录下，严格遵循「功能全称→版本号」二级目录结构，禁止随意调整目录层级、命名规则。
2.  强制目录结构规范（固定不变）：

```Markdown
/（项目根目录）
├── docs/ # 开发文档总根目录
│ ├── 系统设置 / # 一级目录：功能全称（如系统设置、插件市场、用户管理）
│ │ ├── 第一版 / # 二级目录：版本号（如第一版、第二版、V1.0.0）
│ │ │ ├── 文档元信息与全局开发共识 - 第一版.md
│ │ │ ├── 需求规格说明书 - 第一版.md
│ │ │ ├── 系统总体设计 - 第一版.md
│ │ │ ├── 后端详细设计（Rust）- 第一版.md
│ │ │ ├── 前端详细设计（React）- 第一版.md
│ │ │ ├── 测试与验收方案 - 第一版.md
│ │ │ └── 迭代与交付规范 - 第一版.md
│ │ ├── 第二版 / # 二级目录：功能迭代后的新版本号
│ │ │ ├── 文档元信息与全局开发共识 - 第二版.md
│ │ │ ├── 需求规格说明书 - 第二版.md
│ │ │ ├── 系统总体设计 - 第二版.md
│ │ │ ├── 后端详细设计（Rust）- 第二版.md
│ │ │ ├── 前端详细设计（React）- 第二版.md
│ │ │ ├── 测试与验收方案 - 第二版.md
│ │ │ └── 迭代与交付规范 - 第二版.md
│ ├── [其他功能全称]/ # 其他功能的一级目录，结构同上
```

3.  同一个功能的迭代版本，必须新建对应版本号的二级子目录，存放新版7个章节文档，禁止覆盖、删除历史版本目录与文档，保留所有历史版本文档，便于追溯。
4.  文档写入文件系统前，必须先向用户展示完整的文档全文，校验文档是否符合本规则与《设计文档固定大纲》，经用户确认无误、锁定内容后，方可执行写入操作，禁止私自写入未确认的内容，禁止写入时覆盖已锁定的前置文档。

## 5. 全局绝对禁忌

1.  禁止将7大核心文档合并到同一份文档中，禁止单章节内容拆分出多个独立文档
2.  禁止调整、修改、删减《设计文档固定大纲》中定义的固定章节大纲，禁止私自变更大纲顺序
3.  禁止未获得用户明确的文档确认指令、未锁定文档，就跳步进入下一环节
4.  禁止以任何形式修改、覆盖、替换已锁定的前置文档、章节内容
5.  禁止输出与已确认的前置章节内容冲突、与项目现有架构不兼容的设计
6.  禁止修改设计文档的存储目录结构、命名规则、章节负责角色
7.  禁止在设计文档中放入未经过用户确认的内容，禁止私自覆盖历史版本文档
8.  禁止输出无明确约束、有歧义、AI无法直接编码落地的模糊设计
9.  禁止在设计文档全流程中，输出任何代码片段、执行命令、编程相关操作与内容
