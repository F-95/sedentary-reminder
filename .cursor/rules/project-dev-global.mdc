---
alwaysApply: true
---
# 开发环节顶层总则

本规则为开发全流程的最高优先级约束，覆盖新功能开发、老功能迭代两大核心场景，所有开发相关规则、技能、智能体执行动作均不得违反本规则，违者需重新执行对应开发环节。

## 1. 开发任务启动指令唯一规则

1.  所有开发任务的**唯一合法启动指令**为固定格式：「现在开始开XXX功能的第X版」，禁止使用任何其他指令、话术、格式启动开发任务。
2.  指令格式强制约束：
    - 「XXX功能」为功能全称，必须与`/docs/`目录下的功能文件夹名称100%完全一致，禁止简写、别名、错字。
    - 「第X版」为版本号，必须与对应功能文件夹下的版本子目录名称100%完全一致，禁止简写、格式偏差。
    - 指令解析规则：Trae接收到指令后，自动提取「功能全称=XXX功能」「版本号=第X版」，自动匹配设计文档归档路径为`/docs/XXX功能/第X版/`。
3.  指令合法性校验：仅当指令完全符合固定格式、对应路径下存在完整锁定的7份设计文档时，方可启动开发任务；校验不通过，直接终止流程，向用户反馈格式错误或文档缺失问题。

## 2. 开发基准唯一规则

1.  所有开发工作的**唯一合法输入**，是指令匹配路径下「已完成用户全流程确认、内容锁定、完整归档的7份全套设计文档」。
2.  禁止基于未确认、未锁定、不完整的设计文档开展开发，禁止基于口头需求、临时需求、未归档的文档片段、历史版本设计文档开展任何编码工作。
3.  所有代码实现、接口定义、数据结构、交互逻辑、权限控制、错误处理，必须100%匹配对应版本设计文档的要求，禁止私自变更、偏离、删减设计文档中定义的任何内容。

## 3. 单会话单功能单版本规则

1.  一个Trae任务会话，**仅允许执行一条开发启动指令、处理一个功能的一个版本**的开发工作，禁止在同一会话中执行多条启动指令、处理多个功能、同一功能的多个版本。
2.  会话启动后，自动将会话名称修改为与指令完全一致的「XXX功能第X版开发任务」，确保上下文无混杂、全流程可追溯。
3.  多功能、多版本开发必须开启独立会话，分别执行对应启动指令，禁止跨会话复用上下文、禁止上下文混杂。

## 4. 智能体调用刚性规则

1.  所有智能体必须严格按照本规则体系定义的**调用时机、前置条件、职责范围**调用，禁止提前调用、跳步调用、越权调用、无依据调用。
2.  上一环节未完成验收闭环、未输出验收通过报告，绝对禁止调用下一环节的智能体，禁止逆序、并行调用。
3.  每个智能体仅能在自身职责范围内，基于对应设计文档、已锁定的前置环节输出物开展工作，禁止越权干预其他智能体的执行细节。

## 5. 开发流程不可逆规则

1.  开发流程必须严格遵循「指令解析→前置校验→架构评审→后端开发→前端开发→前后端联调→测试验证→需求验收→归档闭环」的顺序执行。
2.  若开发过程中需修改已锁定的设计文档，必须立即终止当前开发流程，重新走设计文档变更与确认流程，完成新版本设计文档锁定后，重新执行新的启动指令，开启新会话开发，禁止在原会话中直接变更开发基准。

## 6. 全局绝对禁忌

1.  禁止使用非固定格式指令启动任何开发任务
2.  禁止无锁定设计文档、无合法指令启动开发工作
3.  禁止开发内容与对应版本设计文档要求不符
4.  禁止单会话处理多功能、多版本开发任务
5.  禁止未按规定时机、前置条件调用智能体
6.  禁止上一环节未验收闭环就跳步进入下一环节
7.  禁止越权干预其他智能体的职责范围内工作
8.  禁止私自修改项目核心架构、全局规范、目录结构
